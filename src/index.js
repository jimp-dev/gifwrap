'use strict';

const { Gif, GifError } = require('./gif');
const { GifFrame } = require('./gifframe');
const { GifCodec } = require('./gifcodec');

module.exports = {
    Gif,
    GifFrame,
    GifCodec,
    GifError
};
